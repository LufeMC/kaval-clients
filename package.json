{
  "name": "kaval-clients",
  "version": "0.1.2",
  "private": true,
  "description": "Open-source client libraries for Kaval — the Node & Python SDKs and the MCP server.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "check": "pnpm run build && pnpm run lint && pnpm run typecheck && pnpm run test",
    "check:release-workflow": "node scripts/check-release-workflow.mjs"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
