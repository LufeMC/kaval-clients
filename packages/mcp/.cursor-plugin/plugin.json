{
  "name": "kaval",
  "version": "0.1.3",
  "description": "The freshness gate for AI agents — pre-action currentness checks that verify a belief is still true against the live world before you act. A thin MCP client over the hosted Kaval API (bring your own key).",
  "author": {
    "name": "Kaval",
    "url": "https://usekaval.com"
  },
  "keywords": ["mcp", "ai", "agents", "freshness", "rag", "verification", "currentness", "memory", "guardrail"],
  "mcpServers": {
    "kaval": {
      "command": "npx",
      "args": ["-y", "@usekaval/mcp"],
      "env": {
        "KAVAL_API_KEY": "${KAVAL_API_KEY}"
      }
    }
  }
}
